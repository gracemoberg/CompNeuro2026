# CompNeuro2026

### Code for APPM 5370, Fall 2026

Each HW assignment has its own folder and files associated with each problem are named accordingly (i.e., files pertaining to problem 5 of HW1 will be named `p5_filename.ext`). If all code for one problem is contained within the file, parts of each problem are labeled in comments.

### Contents
- HW1
  -  `p4_run_pliaf.m`
  -   `p5_find_rheobase.m` (requires `hh_sim.m`)
